{
  "language": "JavaScript",
  "themeColor": "#f7df1e",
  "description": "Master JavaScript from the fundamentals of variables and functions to advanced concepts like asynchronous programming, DOM manipulation, and ES6+ features.",
  "topics": [
    {
      "id": "js-intro",
      "section": "1. JavaScript Basics",
      "title": "Introduction to JavaScript",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Basics</div>\n  <h2>1. Introduction</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Beginner</span>\n    <span class=\"gfg-time\">⏱️ 5 min read</span>\n    <span class=\"gfg-updated\">Updated: July 2026</span>\n  </div>\n\n  <p>JavaScript (JS) is a lightweight, cross-platform programming language used primarily to make web pages interactive and dynamic. Alongside HTML and CSS, it is one of the three core technologies powering the World Wide Web. While HTML structures a webpage and CSS handles the visual styling, JavaScript adds behavior—allowing websites to respond to user actions without reloading the page.</p>\n\n  <h3>The Core Web Layer Cake</h3>\n  <ul>\n    <li><strong>HTML:</strong> Defines the layout and text content.</li>\n    <li><strong>CSS:</strong> Controls design, fonts, and colors.</li>\n    <li><strong>JavaScript:</strong> Powers dynamic elements like dropdown menus, image carousels, and live data updates.</li>\n  </ul>\n\n  <h3>Key Technical Characteristics</h3>\n  <ul>\n    <li><strong>Interpreted / JIT Compiled:</strong> It is traditionally executed line-by-line inside web browsers. Modern browsers use Just-In-Time (JIT) compilation to transform it into fast machine code on the fly.</li>\n    <li><strong>Dynamically Typed:</strong> Variable data types (like numbers or text strings) are automatically determined at runtime, meaning you do not have to explicitly declare them.</li>\n    <li><strong>Event-Driven:</strong> It listens for specific user triggers—such as button clicks, mouse hovers, or keyboard presses—and runs code blocks in response.</li>\n    <li><strong>Single-Threaded & Asynchronous:</strong> It executes one task at a time but can handle operations like fetching web data in the background so the user interface never freezes.</li>\n  </ul>\n\n  <h3>Where is it Used?</h3>\n  <div class=\"gfg-callout gfg-note\">\n    <ul>\n      <li><strong>Client-Side (The Browser):</strong> JavaScript runs directly inside your web browser (like Chrome, Safari, or Firefox). According to MDN Web Docs, it manipulates the Document Object Model (DOM) to update website content instantly. Popular tools used for front-end development include React, Angular, and Vue.js.</li>\n      <li><strong>Server-Side (The Backend):</strong> Through environments like Node.js, developers can use JavaScript outside of a browser. This allows them to build full-scale web servers, manage databases, handle file systems, and create APIs.</li>\n    </ul>\n  </div>\n</div>"
    },
    {
      "id": "js-variables",
      "section": "1. JavaScript Basics",
      "title": "JavaScript Variables & Scope",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Basics</div>\n  <h2>JavaScript Variables, Scope & Hoisting</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Core Foundation</span>\n    <span class=\"gfg-time\">⏱️ 25 min read</span>\n    <span class=\"gfg-updated\">Updated: July 2026</span>\n  </div>\n\n  <p>In JavaScript, variables are the most fundamental building blocks of any program. They act as named containers or storage areas that hold data values during the execution of a script. Think of a variable as a labeled box where you can place a value (like a number or a text string) and retrieve or change it later using its label. Understanding how JavaScript manages variables, allocates memory for them, and defines their accessibility (scope) is critical for mastering the language.</p>\n\n  <p>JavaScript provides three distinct keywords to declare variables: <code>var</code>, <code>let</code>, and <code>const</code>. Each of these keywords behaves differently in terms of scope, hoisting, and reassignment capabilities. In this comprehensive guide, we will explore the history of these keywords, the rules for naming variables, the concepts of scope and the Temporal Dead Zone (TDZ), and the mechanics of hoisting.</p>\n\n  <hr>\n\n  <h3>Rules for Naming Variables</h3>\n  <p>Before diving into how to declare variables, it is important to understand the rules and conventions for naming them (also known as identifiers). In JavaScript, variable names must follow these strict rules:</p>\n  <ul>\n    <li>Names must begin with a letter (a-z, A-Z), an underscore (<code>_</code>), or a dollar sign (<code>$</code>).</li>\n    <li>Subsequent characters can include digits (0-9) in addition to letters, underscores, and dollar signs.</li>\n    <li>Names cannot begin with a digit (e.g., <code>1stPlace</code> is invalid).</li>\n    <li>JavaScript is strictly case-sensitive. Therefore, <code>myVariable</code>, <code>MyVariable</code>, and <code>myvariable</code> are treated as three completely different identifiers.</li>\n    <li>Reserved keywords (like <code>if</code>, <code>else</code>, <code>function</code>, <code>return</code>, <code>var</code>, etc.) cannot be used as variable names.</li>\n  </ul>\n  <div class=\"gfg-callout gfg-tip\">\n    <h4>💡 Industry Best Practice: Naming Conventions</h4>\n    <p>The industry standard for naming variables in JavaScript is <strong>camelCase</strong>. In camelCase, the first letter is lowercase, and every subsequent word starts with an uppercase letter (e.g., <code>userFirstName</code>, <code>totalAmountDue</code>). Additionally, variables should have descriptive, semantic names that clearly indicate what data they hold. Avoid single-letter variables like <code>x</code> or <code>y</code> unless they are used as short-lived loop counters.</p>\n  </div>\n\n  <hr>\n\n  <h3>1. The <code>var</code> Keyword: The Legacy Way</h3>\n  <p>From the inception of JavaScript in 1995 until the release of ECMAScript 2015 (ES6), the <code>var</code> keyword was the only way to declare variables. While it is still fully supported for backwards compatibility, it is generally avoided in modern development due to its confusing scoping rules.</p>\n  \n  <h4>Function Scope vs Global Scope</h4>\n  <p>Variables declared with <code>var</code> are either <strong>function-scoped</strong> or <strong>globally scoped</strong>. They are never block-scoped. This means that if a <code>var</code> variable is declared inside a function, it is only accessible within that function. However, if it is declared inside a block (like an <code>if</code> statement or a <code>for</code> loop), it \"leaks\" out of that block and becomes accessible to the entire surrounding function (or the global scope if not in a function).</p>\n\n  <pre><code>function testVar() {\n    if (true) {\n        var leakedVar = \"I am outside the block!\";\n    }\n    // leakedVar is accessible here!\n    console.log(leakedVar); \n}\ntestVar();</code></pre>\n\n  <h4>Redeclaration and Updating</h4>\n  <p>Another quirk of <code>var</code> is that it allows you to redeclare the same variable multiple times within the same scope without throwing an error. This can lead to significant bugs if a developer accidentally overwrites a variable they did not realize was already declared.</p>\n  <pre><code>var username = \"Alice\";\nvar username = \"Bob\"; // No error thrown!\nconsole.log(username); // \"Bob\"</code></pre>\n\n  <hr>\n\n  <h3>2. The <code>let</code> Keyword: Modern Variable Declaration</h3>\n  <p>To address the shortcomings of <code>var</code>, ES6 introduced the <code>let</code> keyword. It is the modern standard for declaring variables whose values are expected to change (mutate) over time.</p>\n\n  <h4>Block Scope</h4>\n  <p>The most significant difference between <code>let</code> and <code>var</code> is that <code>let</code> is <strong>block-scoped</strong>. A block is any code defined between a set of curly braces <code>{}</code>. If a <code>let</code> variable is declared inside a block, it is strictly confined to that block and cannot be accessed from the outside.</p>\n  <pre><code>if (true) {\n    let secureVar = \"I am trapped in this block!\";\n    console.log(secureVar); // Works fine\n}\n// console.log(secureVar); // ReferenceError: secureVar is not defined</code></pre>\n\n  <h4>No Redeclaration</h4>\n  <p>Unlike <code>var</code>, <code>let</code> does not allow you to redeclare a variable within the same scope. This strictness prevents accidental variable overwrites.</p>\n  <pre><code>let score = 10;\n// let score = 20; // SyntaxError: Identifier 'score' has already been declared\nscore = 20; // Reassigning the value is perfectly fine</code></pre>\n\n  <hr>\n\n  <h3>3. The <code>const</code> Keyword: Constants and Immutability</h3>\n  <p>The <code>const</code> keyword, also introduced in ES6, is used to declare variables whose identifiers should never be reassigned. Like <code>let</code>, <code>const</code> is strictly <strong>block-scoped</strong> and cannot be redeclared.</p>\n\n  <h4>Strict Initialization</h4>\n  <p>When declaring a variable with <code>const</code>, you must initialize it with a value at the exact moment of declaration. You cannot declare an uninitialized <code>const</code> variable.</p>\n  <pre><code>const PI = 3.14159; // Correct\n// const GRAVITY; // SyntaxError: Missing initializer in const declaration</code></pre>\n\n  <h4>Immutability Nuances</h4>\n  <p>It is crucial to understand that <code>const</code> does not mean the value itself is entirely immutable—it only means the variable identifier cannot be reassigned to a new memory address. If a <code>const</code> variable holds a primitive value (like a number or string), it cannot be changed. However, if it holds a reference type (like an Object or an Array), the contents of that object or array can still be mutated.</p>\n  <pre><code>const user = { name: \"Alice\", age: 25 };\n// user = { name: \"Bob\", age: 30 }; // TypeError: Assignment to constant variable.\n\nuser.age = 26; // Perfectly valid! We are mutating the object, not reassigning the identifier.\nconsole.log(user.age); // 26</code></pre>\n\n  <hr>\n\n  <h3>Deep Dive: Understanding Hoisting</h3>\n  <p>Hoisting is JavaScript's default behavior of moving variable and function declarations to the top of their respective scopes during the compilation phase (before the code is executed). This means that you can technically reference a variable before the line of code where it is declared.</p>\n\n  <h4>Hoisting with <code>var</code></h4>\n  <p>When variables are declared with <code>var</code>, their declarations are hoisted to the top, and they are immediately initialized with the default value of <code>undefined</code>.</p>\n  <pre><code>console.log(myVar); // Outputs: undefined (no error)\nvar myVar = \"Hello World\";\nconsole.log(myVar); // Outputs: \"Hello World\"</code></pre>\n\n  <h4>Hoisting with <code>let</code> and <code>const</code> (The Temporal Dead Zone)</h4>\n  <p>Variables declared with <code>let</code> and <code>const</code> are also hoisted to the top of their block scope. However, unlike <code>var</code>, they are <strong>not initialized</strong> with <code>undefined</code>. Instead, they remain in a state known as the <strong>Temporal Dead Zone (TDZ)</strong> from the start of the block until the engine reaches the exact line where the variable is declared and initialized.</p>\n  <p>If you attempt to access a <code>let</code> or <code>const</code> variable while it is in the TDZ, JavaScript will throw a <code>ReferenceError</code>.</p>\n  <pre><code>// console.log(myLet); // ReferenceError: Cannot access 'myLet' before initialization\nlet myLet = \"Modern JavaScript\";</code></pre>\n  <p>The TDZ is a protective mechanism designed to catch bugs. It forces developers to write cleaner, more predictable code by ensuring variables are only used after they have been properly declared.</p>\n\n  <hr>\n\n  <h3>Deep Dive: Variable Shadowing</h3>\n  <p>Variable shadowing occurs when a variable declared within a certain scope (like an inner block) has the same name as a variable declared in an outer scope. The inner variable \"shadows\" the outer variable, meaning any references to that variable name within the inner scope will point to the inner variable.</p>\n  <pre><code>let message = \"Global Message\";\nif (true) {\n    let message = \"Local Message\"; // Shadows the outer variable\n    console.log(message); // \"Local Message\"\n}\nconsole.log(message); // \"Global Message\"</code></pre>\n  <p>It is important to note that you can shadow a <code>var</code> variable with a <code>let</code> variable (known as valid shadowing), but attempting to shadow a <code>let</code> variable with a <code>var</code> variable within the same block scope will result in an \"Illegal Shadowing\" syntax error because <code>var</code> tries to register itself in the outer function scope where the <code>let</code> variable already exists.</p>\n\n  <hr>\n  <h3>Conclusion & Best Practices</h3>\n  <p>Modern JavaScript development heavily favors the use of <code>let</code> and <code>const</code>. The community consensus is to <strong>always use <code>const</code> by default</strong>. This communicates to other developers that a variable is not meant to change, making the code easier to reason about. Only switch to <code>let</code> when you explicitly know that the variable will need to be reassigned (such as counters in <code>for</code> loops or toggles for boolean states). The <code>var</code> keyword should be avoided entirely in new projects to prevent scoping bugs and unpredictable behavior caused by global leakage and undefined hoisting.</p>\n</div>"
    },
    {
      "id": "js-datatypes",
      "section": "1. JavaScript Basics",
      "title": "JavaScript Data Types & Type Coercion",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Basics</div>\n  <h2>JavaScript Data Types & Type Coercion</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Core Foundation</span>\n    <span class=\"gfg-time\">⏱️ 25 min read</span>\n    <span class=\"gfg-updated\">Updated: July 2026</span>\n  </div>\n\n  <p>In computer science, a \"data type\" is an attribute of data that tells the compiler or interpreter how the programmer intends to use the data. In JavaScript, data types dictate what operations can be performed on a variable and how the data is stored in the system's memory.</p>\n\n  <p>JavaScript is a <strong>dynamically typed</strong> (also referred to as loosely typed) language. This means that variables in JavaScript are not directly bound to any specific data type. You do not need to explicitly declare whether a variable will hold a number, a string, or an object. The JavaScript engine automatically determines the data type during execution (runtime). Furthermore, a single variable can hold a string at one moment, and later be reassigned to hold a number or an array without throwing any errors.</p>\n\n  <pre><code>let dynamicVar = \"Hello there!\"; // It is currently a String\ndynamicVar = 42; // Now it is a Number\ndynamicVar = true; // Now it is a Boolean</code></pre>\n\n  <p>JavaScript data types are broadly categorized into two distinct groups based on how they are stored in memory and how they are accessed: <strong>Primitive Data Types</strong> and <strong>Non-Primitive (Reference) Data Types</strong>.</p>\n\n  <hr>\n\n  <h2>Part 1: Primitive Data Types</h2>\n  <p>Primitive data types are the most basic, lowest-level forms of data. They represent a single, simple value. In JavaScript, primitives possess two critical characteristics:</p>\n  <ol>\n    <li><strong>Immutability:</strong> The actual primitive value cannot be altered or modified once it is created. If you alter a string variable, you are not modifying the original string; you are destroying the old value and creating a brand new string in memory.</li>\n    <li><strong>Stored by Value:</strong> Primitives are stored directly in the \"Stack\" memory space. When you assign a primitive to a new variable, JavaScript creates a strict copy of the actual value. Modifying the copy does not affect the original variable.</li>\n  </ol>\n  \n  <p>Modern JavaScript defines seven primitive data types:</p>\n\n  <h3>1. Number</h3>\n  <p>Unlike languages like C or Java that have distinct types for integers (int), floating-point numbers (float), and double-precision floats (double), JavaScript uses a single <code>Number</code> type for all numbers. It follows the IEEE 754 standard as a double-precision 64-bit floating-point format.</p>\n  <pre><code>let intAge = 25;       // Integer\nlet floatPrice = 9.99; // Floating point</code></pre>\n  <p>The Number type also includes three special symbolic values:</p>\n  <ul>\n    <li><code>Infinity</code>: Represents mathematical infinity (e.g., the result of dividing a non-zero number by 0).</li>\n    <li><code>-Infinity</code>: Represents negative infinity.</li>\n    <li><code>NaN</code> (Not-a-Number): A special value representing a computational error. It is generated when a mathematical operation fails, such as attempting to divide a string of text by a number (<code>\"hello\" / 5</code>). Interestingly, <code>typeof NaN</code> evaluates to <code>\"number\"</code>.</li>\n  </ul>\n\n  <h3>2. String</h3>\n  <p>A string represents textual data—a sequence of characters. Strings must be enclosed in quotes. You can use single quotes (<code>'...'</code>), double quotes (<code>\"...\"</code>), or backticks (<code>`...`</code>).</p>\n  <p>Backticks, introduced in ES6, allow for <strong>Template Literals</strong>. Template literals support multi-line strings and string interpolation (embedding variables or expressions directly into the string using <code>${}</code> syntax), making string concatenation significantly cleaner.</p>\n  <pre><code>let single = 'Single quotes work.';\nlet double = \"Double quotes work too.\";\nlet user = \"Alice\";\nlet greeting = `Welcome back, ${user}!`; // Template Literal</code></pre>\n\n  <h3>3. Boolean</h3>\n  <p>A boolean represents a logical entity that can strictly hold one of two values: <code>true</code> or <code>false</code>. Booleans are the backbone of decision-making and conditional testing in programming (such as <code>if...else</code> blocks and <code>while</code> loops).</p>\n\n  <h3>4. Undefined</h3>\n  <p><code>undefined</code> is a special primitive type that has exactly one value: <code>undefined</code>. When a variable is declared using <code>var</code> or <code>let</code> but has not yet been assigned a specific value by the programmer, the JavaScript engine automatically initializes it with the value of <code>undefined</code>. It represents an unintentional absence of a value.</p>\n  <pre><code>let userAge;\nconsole.log(userAge); // Outputs: undefined</code></pre>\n\n  <h3>5. Null</h3>\n  <p>While <code>undefined</code> implies a variable hasn't been initialized, <code>null</code> is used to represent the <strong>intentional, explicit absence</strong> of any object value. Programmers assign <code>null</code> to variables to deliberately clear them or indicate that they hold \"nothing\".</p>\n  <div class=\"gfg-callout gfg-note\">\n    <h4>📝 The <code>typeof null</code> Bug</h4>\n    <p>Due to a legacy bug dating back to the very first version of JavaScript, evaluating <code>typeof null</code> incorrectly returns the string <code>\"object\"</code>. Despite this behavior, <code>null</code> is absolutely a primitive type, not an object. The bug was never fixed because doing so would break millions of existing websites that rely on that specific behavior.</p>\n  </div>\n\n  <h3>6. BigInt</h3>\n  <p>The standard <code>Number</code> type is only precise up to <code>2^53 - 1</code> (known as <code>Number.MAX_SAFE_INTEGER</code>). For mathematical operations involving astronomically large numbers (such as cryptographic calculations or high-precision timestamps), JavaScript introduced <code>BigInt</code> in ES2020. You create a BigInt by appending the letter <code>n</code> to the end of an integer, or by calling the <code>BigInt()</code> constructor.</p>\n  <pre><code>let normalMax = 9007199254740991;\nlet hugeNumber = 9007199254740992n; // BigInt</code></pre>\n\n  <h3>7. Symbol</h3>\n  <p>Introduced in ES6, a <code>Symbol</code> is a completely unique and immutable primitive value. Symbols are primarily used as unique property keys inside objects to guarantee that properties do not conflict or overwrite each other, even if they have the same string description. Every time you invoke <code>Symbol()</code>, it generates a brand new, unique identifier.</p>\n  <pre><code>let symA = Symbol(\"id\");\nlet symB = Symbol(\"id\");\nconsole.log(symA === symB); // false</code></pre>\n\n  <hr>\n\n  <h2>Part 2: Non-Primitive (Reference) Data Types</h2>\n  <p>Unlike primitives, Non-Primitive data types (broadly referred to as Objects) are complex data structures that can hold collections of values or complex entities. They possess two contrasting characteristics:</p>\n  <ol>\n    <li><strong>Mutability:</strong> The contents of reference types can be altered (properties can be added, updated, or deleted) without destroying the original structure.</li>\n    <li><strong>Stored by Reference:</strong> Objects are stored in the \"Heap\" memory space. The variable holding the object does not store the object directly; it stores a \"reference\" (or memory address) pointing to the location in the heap where the object lives. If you assign an object to a new variable, both variables point to the exact same memory address. Modifying one will modify the other.</li>\n  </ol>\n\n  <h3>1. Object Literal</h3>\n  <p>The standard object is an unordered collection of key-value pairs. Keys (properties) are usually strings or symbols, and values can be absolutely any data type, including functions (which act as object methods) or other nested objects.</p>\n  <pre><code>let developer = {\n    name: \"John Doe\",\n    skills: [\"JavaScript\", \"React\", \"Node\"],\n    isActive: true,\n    greet: function() {\n        console.log(\"Hello!\");\n    }\n};</code></pre>\n\n  <h3>2. Array</h3>\n  <p>An Array is a specialized type of object used to store multiple, ordered values in a single variable. Arrays are strictly zero-indexed (the first element is at index 0). Because arrays are technically objects, <code>typeof []</code> returns <code>\"object\"</code>. To specifically verify if a variable is an array, you must use <code>Array.isArray(myArray)</code>.</p>\n\n  <h3>3. Function</h3>\n  <p>In JavaScript, functions are considered \"first-class citizens,\" meaning they are just a specific type of callable object. Because they are objects, you can assign functions to variables, pass them as arguments to other functions (callbacks), and return them from functions. Interestingly, <code>typeof function(){}</code> returns a special string <code>\"function\"</code>, even though it is fundamentally an object.</p>\n\n  <hr>\n\n  <h2>Part 3: Type Coercion and Truthy/Falsy</h2>\n\n  <h3>Implicit vs Explicit Coercion</h3>\n  <p><strong>Implicit Type Coercion</strong> occurs when JavaScript automatically converts a value from one data type to another to successfully execute an operation. This is a common source of bugs for beginners.</p>\n  <pre><code>console.log(\"5\" + 3); // \"53\" (The + operator prefers string concatenation; 3 is coerced to \"3\")\nconsole.log(\"5\" - 3); // 2 (The - operator only does math; \"5\" is coerced to the number 5)</code></pre>\n  \n  <p><strong>Explicit Type Conversion</strong> (or Type Casting) occurs when a developer intentionally converts data using built-in functions like <code>Number()</code>, <code>String()</code>, or <code>Boolean()</code>. This is the recommended practice for predictable code.</p>\n\n  <h3>Truthy and Falsy Values</h3>\n  <p>In Boolean contexts (like an <code>if</code> statement), JavaScript forces all values to be evaluated as either <code>true</code> or <code>false</code>. Values that evaluate to false are known as <strong>Falsy</strong>. There are exactly six falsy values in JavaScript:</p>\n  <ul>\n    <li><code>false</code></li>\n    <li><code>0</code> (including <code>-0</code> and <code>0n</code>)</li>\n    <li><code>\"\"</code> (an empty string)</li>\n    <li><code>null</code></li>\n    <li><code>undefined</code></li>\n    <li><code>NaN</code></li>\n  </ul>\n  <p>Absolutely every other value in JavaScript is considered <strong>Truthy</strong>, including empty arrays (<code>[]</code>), empty objects (<code>{}</code>), and strings with empty spaces (<code>\" \"</code>).</p>\n\n  <hr>\n  <h3>Conclusion</h3>\n  <p>A profound understanding of Data Types—differentiating between the immutable, value-based nature of Primitives and the mutable, reference-based nature of Objects—is mandatory for mastering JavaScript. Coupled with a solid grasp of how JavaScript automatically coerces types and evaluates truthy/falsy conditions, developers can architect highly stable, bug-free applications.</p>\n</div>"
    },
    {
      "id": "js-operators",
      "section": "1. JavaScript Basics",
      "title": "Operators",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Basics</div>\n  <h2>JavaScript Operators</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Core Foundation</span>\n    <span class=\"gfg-time\">⏱️ 25 min read</span>\n    <span class=\"gfg-updated\">Updated: July 2026</span>\n  </div>\n\n  <p>In JavaScript, an <strong>operator</strong> is a special symbol used to perform operations on operands (values and variables). For example, in the expression <code>2 + 3</code>, the numbers <code>2</code> and <code>3</code> are the operands, and the <code>+</code> symbol is the operator. Operators are the foundation of any programming language, allowing you to manipulate data, perform mathematical calculations, compare values, and assign data to variables.</p>\n\n  <p>JavaScript provides a rich set of operators that can be categorized based on their functionality. In this comprehensive guide, we will explore the different types of JavaScript operators, their syntax, and how they behave in various contexts.</p>\n\n  <hr>\n\n  <h3>1. Arithmetic Operators</h3>\n  <p>Arithmetic operators are used to perform mathematical calculations on numbers. JavaScript supports all standard arithmetic operations.</p>\n  <table class=\"gfg-table\">\n    <thead>\n      <tr>\n        <th>Operator</th>\n        <th>Description</th>\n        <th>Example (Let a=10, b=3)</th>\n      </tr>\n    </thead>\n    <tbody>\n      <tr>\n        <td><code>+</code></td>\n        <td>Addition: Adds two numbers. If one operand is a string, it concatenates them.</td>\n        <td><code>a + b</code> returns 13</td>\n      </tr>\n      <tr>\n        <td><code>-</code></td>\n        <td>Subtraction: Subtracts the right operand from the left operand.</td>\n        <td><code>a - b</code> returns 7</td>\n      </tr>\n      <tr>\n        <td><code>*</code></td>\n        <td>Multiplication: Multiplies two numbers.</td>\n        <td><code>a * b</code> returns 30</td>\n      </tr>\n      <tr>\n        <td><code>/</code></td>\n        <td>Division: Divides the left operand by the right operand.</td>\n        <td><code>a / b</code> returns 3.3333</td>\n      </tr>\n      <tr>\n        <td><code>%</code></td>\n        <td>Modulus (Remainder): Returns the division remainder.</td>\n        <td><code>a % b</code> returns 1</td>\n      </tr>\n      <tr>\n        <td><code>**</code></td>\n        <td>Exponentiation: Raises the first operand to the power of the second.</td>\n        <td><code>a ** b</code> returns 1000</td>\n      </tr>\n      <tr>\n        <td><code>++</code></td>\n        <td>Increment: Increases an integer value by one.</td>\n        <td><code>a++</code> returns 11</td>\n      </tr>\n      <tr>\n        <td><code>--</code></td>\n        <td>Decrement: Decreases an integer value by one.</td>\n        <td><code>a--</code> returns 9</td>\n      </tr>\n    </tbody>\n  </table>\n  <div class=\"gfg-syntax-box\">\n    <div class=\"syntax-title\">String Concatenation with +</div>\n    <pre><code>let x = 5 + 5; // 10\nlet y = \"5\" + 5; // \"55\" (The + operator prefers string concatenation)\nlet z = \"Hello\" + \" \" + \"World\"; // \"Hello World\"</code></pre>\n  </div>\n\n  <hr>\n\n  <h3>2. Assignment Operators</h3>\n  <p>Assignment operators are used to assign values to JavaScript variables. The most common assignment operator is the equals sign (<code>=</code>), which assigns the value on the right to the variable on the left.</p>\n  <ul>\n    <li><code>=</code> : Assign (x = y)</li>\n    <li><code>+=</code> : Add and Assign (x += y is equivalent to x = x + y)</li>\n    <li><code>-=</code> : Subtract and Assign (x -= y is equivalent to x = x - y)</li>\n    <li><code>*=</code> : Multiply and Assign (x *= y is equivalent to x = x * y)</li>\n    <li><code>/=</code> : Divide and Assign (x /= y is equivalent to x = x / y)</li>\n    <li><code>%=</code> : Modulus and Assign (x %= y is equivalent to x = x % y)</li>\n    <li><code>**=</code> : Exponentiation and Assign (x **= y is equivalent to x = x ** y)</li>\n  </ul>\n  <pre><code>let score = 10;\nscore += 5; // score is now 15\nscore *= 2; // score is now 30</code></pre>\n\n  <hr>\n\n  <h3>3. Comparison (Relational) Operators</h3>\n  <p>Comparison operators are used in logical statements to determine equality or difference between variables or values. They evaluate to a boolean value (<code>true</code> or <code>false</code>).</p>\n  \n  <h4>Loose Equality vs Strict Equality</h4>\n  <p>Understanding the difference between loose (<code>==</code>) and strict (<code>===</code>) equality is crucial in JavaScript to prevent unexpected bugs caused by automatic type coercion.</p>\n  <ul>\n    <li><strong><code>==</code> (Loose Equality):</strong> Compares two values for equality <em>after</em> converting both values to a common type. For example, <code>5 == \"5\"</code> evaluates to <code>true</code>.</li>\n    <li><strong><code>===</code> (Strict Equality):</strong> Compares both the value <strong>and the type</strong>. No type conversion is performed. <code>5 === \"5\"</code> evaluates to <code>false</code> because one is a Number and the other is a String.</li>\n    <li><strong><code>!=</code> (Loose Inequality):</strong> Returns true if the operands are not equal (with type coercion).</li>\n    <li><strong><code>!==</code> (Strict Inequality):</strong> Returns true if the operands are not equal and/or not of the same type.</li>\n  </ul>\n  <div class=\"gfg-callout gfg-tip\">\n    <h4>💡 Industry Best Practice</h4>\n    <p>Always use strict equality (<code>===</code>) and strict inequality (<code>!==</code>) unless you have a highly specific reason to allow type coercion. This guarantees that your comparisons are predictable and bug-free.</p>\n  </div>\n  \n  <h4>Relational Operators</h4>\n  <ul>\n    <li><code>&gt;</code> (Greater than)</li>\n    <li><code>&lt;</code> (Less than)</li>\n    <li><code>&gt;=</code> (Greater than or equal to)</li>\n    <li><code>&lt;=</code> (Less than or equal to)</li>\n  </ul>\n\n  <hr>\n\n  <h3>4. Logical Operators</h3>\n  <p>Logical operators are used to determine the logic between variables or values. They are typically used with boolean (logical) values to form complex conditional statements.</p>\n  <table class=\"gfg-table\">\n    <thead>\n      <tr>\n        <th>Operator</th>\n        <th>Description</th>\n        <th>Example</th>\n      </tr>\n    </thead>\n    <tbody>\n      <tr>\n        <td><code>&&</code> (Logical AND)</td>\n        <td>Returns true if <strong>both</strong> operands are true.</td>\n        <td><code>(6 &gt; 5 && 3 &gt; 2)</code> returns true</td>\n      </tr>\n      <tr>\n        <td><code>||</code> (Logical OR)</td>\n        <td>Returns true if <strong>at least one</strong> operand is true.</td>\n        <td><code>(6 &gt; 5 || 3 &lt; 2)</code> returns true</td>\n      </tr>\n      <tr>\n        <td><code>!</code> (Logical NOT)</td>\n        <td>Reverses the boolean state of its operand.</td>\n        <td><code>!(6 &gt; 5)</code> returns false</td>\n      </tr>\n    </tbody>\n  </table>\n\n  <h4>Short-Circuit Evaluation</h4>\n  <p>JavaScript logical operators evaluate from left to right and utilize \"short-circuiting\":</p>\n  <ul>\n    <li>With <code>&&</code>, if the first operand is falsy, it immediately returns the first operand and doesn't even evaluate the second one.</li>\n    <li>With <code>||</code>, if the first operand is truthy, it immediately returns the first operand and skips the second.</li>\n  </ul>\n\n  <hr>\n\n  <h3>5. Bitwise Operators</h3>\n  <p>Bitwise operators treat their operands as a set of 32 bits (zeros and ones), rather than as decimal, hexadecimal, or octal numbers. They perform operations on their binary representations but return standard JavaScript numerical values.</p>\n  <ul>\n    <li><code>&amp;</code> (Bitwise AND)</li>\n    <li><code>|</code> (Bitwise OR)</li>\n    <li><code>^</code> (Bitwise XOR)</li>\n    <li><code>~</code> (Bitwise NOT)</li>\n    <li><code>&lt;&lt;</code> (Left Shift)</li>\n    <li><code>&gt;&gt;</code> (Sign-Propagating Right Shift)</li>\n    <li><code>&gt;&gt;&gt;</code> (Zero-Fill Right Shift)</li>\n  </ul>\n  <pre><code>let bitwiseAnd = 5 & 1; // 0101 & 0001 = 0001 (returns 1)\nlet bitwiseOr = 5 | 1;  // 0101 | 0001 = 0101 (returns 5)</code></pre>\n\n  <hr>\n\n  <h3>6. The Ternary (Conditional) Operator</h3>\n  <p>The ternary operator is the only JavaScript operator that takes three operands. It is frequently used as a one-line shorthand for an <code>if...else</code> statement.</p>\n  <div class=\"gfg-syntax-box\">\n    <div class=\"syntax-title\">Syntax</div>\n    <pre><code>condition ? expressionIfTrue : expressionIfFalse;</code></pre>\n  </div>\n  <pre><code>let age = 20;\nlet canDrive = (age &gt;= 16) ? \"Yes\" : \"No\";\nconsole.log(canDrive); // \"Yes\"</code></pre>\n\n  <hr>\n\n  <h3>7. Modern ES6+ Operators</h3>\n  \n  <h4>Nullish Coalescing Operator (<code>??</code>)</h4>\n  <p>Introduced in ES2020, the nullish coalescing operator (<code>??</code>) is a logical operator that returns its right-hand side operand when its left-hand side operand is <code>null</code> or <code>undefined</code>, and otherwise returns its left-hand side operand. This is a safer alternative to the logical OR (<code>||</code>) operator when you want to provide default values, because <code>||</code> returns the right side for <em>any</em> falsy value (like <code>0</code> or <code>\"\"</code>).</p>\n  <pre><code>let count = 0;\nlet text = \"\";\n\nlet defaultWithOr = count || 42; // Returns 42 (0 is falsy)\nlet defaultWithNullish = count ?? 42; // Returns 0 (0 is not null or undefined)</code></pre>\n\n  <h4>Optional Chaining Operator (<code>?.</code>)</h4>\n  <p>The optional chaining operator (<code>?.</code>) allows you to read the value of a property located deep within a chain of connected objects without having to explicitly validate that each reference in the chain is valid. If a reference is <code>null</code> or <code>undefined</code>, the expression short-circuits and evaluates to <code>undefined</code> instead of throwing a massive error.</p>\n  <pre><code>let user = {\n    profile: {\n        email: \"alice@example.com\"\n    }\n};\n\n// Instead of: let zip = user && user.address && user.address.zipcode;\nlet zip = user?.address?.zipcode; \nconsole.log(zip); // undefined (No error thrown!)</code></pre>\n\n  <hr>\n\n  <h3>Conclusion</h3>\n  <p>Operators are the foundational glue that allows JavaScript developers to write dynamic, logical, and robust code. From executing basic arithmetic to safely traversing complex objects using modern optional chaining, mastering these operators ensures you can handle data transformation and application state management efficiently and safely.</p>\n\n</div>"
    },
    {
      "id": "js-arrays",
      "section": "2. Functions & Data Structures",
      "title": "Arrays & Array Methods",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Data Structures</div>\n  <h2>Arrays & Array Methods</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Essential</span>\n    <span class=\"gfg-time\">⏱️ 10 min read</span>\n    <span class=\"gfg-updated\">Updated: July 2026</span>\n  </div>\n\n  <p>Arrays are special variables that can hold more than one value at a time. JavaScript arrays are zero-indexed and can store mixed data types.</p>\n\n  <h3>Basic Array Operations</h3>\n  <pre><code>const fruits = [\"Apple\", \"Banana\", \"Orange\"];\n\n// Accessing elements\nconsole.log(fruits[0]); // \"Apple\"\n\n// Modifying elements\nfruits[1] = \"Mango\";\n\n// Length property\nconsole.log(fruits.length); // 3\n\n// Adding/Removing elements\nfruits.push(\"Grape\");    // Adds to the end\nfruits.pop();            // Removes from the end\nfruits.unshift(\"Kiwi\");  // Adds to the beginning\nfruits.shift();          // Removes from the beginning</code></pre>\n\n  <h3>Higher-Order Array Methods</h3>\n  <p>Modern JavaScript relies heavily on functional array methods that iterate over elements.</p>\n  \n  <table class=\"gfg-table\">\n    <thead>\n      <tr>\n        <th>Method</th>\n        <th>Description</th>\n        <th>Returns</th>\n      </tr>\n    </thead>\n    <tbody>\n      <tr>\n        <td><code>map()</code></td>\n        <td>Creates a new array populated with the results of calling a provided function on every element.</td>\n        <td>New Array</td>\n      </tr>\n      <tr>\n        <td><code>filter()</code></td>\n        <td>Creates a new array with all elements that pass the test implemented by the provided function.</td>\n        <td>New Array</td>\n      </tr>\n      <tr>\n        <td><code>reduce()</code></td>\n        <td>Executes a reducer function on each element, resulting in a single output value.</td>\n        <td>Single Value</td>\n      </tr>\n      <tr>\n        <td><code>forEach()</code></td>\n        <td>Executes a provided function once for each array element.</td>\n        <td>undefined</td>\n      </tr>\n    </tbody>\n  </table>\n\n  <div class=\"gfg-syntax-box\">\n    <div class=\"syntax-title\">Map, Filter, Reduce Example</div>\n    <pre><code>const numbers = [1, 2, 3, 4, 5];\n\n// map: Double the numbers\nconst doubled = numbers.map(num => num * 2);\n// [2, 4, 6, 8, 10]\n\n// filter: Keep only even numbers\nconst evens = numbers.filter(num => num % 2 === 0);\n// [2, 4]\n\n// reduce: Sum all numbers\nconst sum = numbers.reduce((accumulator, current) => accumulator + current, 0);\n// 15</code></pre>\n  </div>\n</div>"
    },
    {
      "id": "js-objects",
      "section": "2. Functions & Data Structures",
      "title": "Objects & 'this' Keyword",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Data Structures</div>\n  <h2>Objects & the 'this' Keyword</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Core Concept</span>\n    <span class=\"gfg-time\">⏱️ 9 min read</span>\n    <span class=\"gfg-updated\">Updated: July 2026</span>\n  </div>\n\n  <p>Objects are collections of key-value pairs. They are the most important data type in JavaScript for storing structured data.</p>\n\n  <h3>Creating and Accessing Objects</h3>\n  <pre><code>const user = {\n  firstName: \"Jane\",\n  lastName: \"Doe\",\n  age: 25,\n  isAdmin: false\n};\n\n// Dot notation\nconsole.log(user.firstName); // \"Jane\"\n\n// Bracket notation (useful for dynamic keys)\nconst key = \"lastName\";\nconsole.log(user[key]); // \"Doe\"\n\n// Adding/Modifying properties\nuser.email = \"jane@example.com\";\nuser.age = 26;</code></pre>\n\n  <h3>Object Methods & 'this'</h3>\n  <p>Functions stored as object properties are called methods. The <code>this</code> keyword refers to the object executing the current function.</p>\n  \n  <div class=\"gfg-callout gfg-note\">\n    <h4>⚠️ Arrow Functions and 'this'</h4>\n    <p>Arrow functions do not have their own <code>this</code> binding. They inherit <code>this</code> from the enclosing lexical scope. Therefore, <strong>do not use arrow functions as object methods</strong> if you need to access object properties via <code>this</code>.</p>\n  </div>\n\n  <pre><code>const person = {\n  name: \"Bob\",\n  greet: function() {\n    // 'this' refers to the person object\n    return `Hello, my name is ${this.name}`;\n  },\n  // Shorthand syntax (ES6)\n  sayBye() {\n    return `Goodbye from ${this.name}`;\n  },\n  // BAD: Arrow function for method\n  badGreet: () => {\n    return `Hi, I am ${this.name}`; // this.name will be undefined!\n  }\n};\n\nconsole.log(person.greet()); // \"Hello, my name is Bob\"</code></pre>\n</div>"
    },
    {
      "id": "js-functions-basics",
      "section": "3. Functions",
      "title": "Basics & Types",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Functions</div>\n  <h2>3.1 JavaScript Functions: Basics & Types</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Core Foundation</span>\n    <span class=\"gfg-time\">⏱️ 15 min read</span>\n  </div>\n\n  <h3>Introduction to Functions</h3>\n  <p>A JavaScript function is a block of code designed to perform a particular task. A function is executed when \"something\" invokes it (calls it). Functions are a fundamental aspect of any programming language because they allow for code reusability. Instead of writing the same logic multiple times, you write it once inside a function and invoke that function wherever needed.</p>\n\n  <p>In JavaScript, functions are \"first-class objects\". This means they can be treated like any other variable: they can be assigned to variables, passed as arguments to other functions, and returned from other functions. This powerful feature enables functional programming paradigms within JavaScript.</p>\n\n  <h3>1. Function Declarations</h3>\n  <p>The standard way to declare a function is by using the <code>function</code> keyword, followed by a name, followed by parentheses <code>()</code>. The code to be executed is placed inside curly brackets <code>{}</code>. A unique feature of function declarations in JavaScript is \"hoisting\". The JavaScript engine moves function declarations to the top of their scope before the code executes, meaning you can call a function before it is physically defined in your code.</p>\n  <pre><code>// Calling the function before declaration (Hoisting works!)\ngreetUser(\"Alice\");\n\nfunction greetUser(name) {\n    console.log(\"Welcome back, \" + name);\n}</code></pre>\n\n  <h3>2. Function Expressions</h3>\n  <p>A function expression defines a function within an expression. These functions can be anonymous (without a name) and are stored in variables. Unlike function declarations, function expressions are <strong>not hoisted</strong>. You must define them before calling them.</p>\n  <pre><code>const multiply = function(a, b) {\n    return a * b;\n};\nconsole.log(multiply(5, 4)); // Outputs: 20</code></pre>\n\n  <h3>3. Arrow Functions</h3>\n  <p>Introduced in ES6, arrow functions provide a much more concise syntax for writing function expressions. They are particularly useful for short, single-line operations and callbacks. Beyond syntax, arrow functions differ critically in how they handle the <code>this</code> keyword. Traditional functions bind their own <code>this</code> based on how they are called, whereas arrow functions inherit <code>this</code> from the surrounding lexical scope.</p>\n  <pre><code>// Standard Arrow Function\nconst add = (x, y) =&gt; {\n    return x + y;\n};\n\n// Implicit Return (One-liner)\nconst square = num =&gt; num * num;</code></pre>\n\n  <h3>4. Immediately Invoked Function Expressions (IIFE)</h3>\n  <p>An IIFE is a function that runs as soon as it is defined. It's a common design pattern used to create a private scope, ensuring that variables declared inside the IIFE do not pollute the global namespace. It is wrapped in parentheses and immediately followed by another set of parentheses to invoke it.</p>\n  <pre><code>(function() {\n    let privateData = \"Secret\";\n    console.log(\"IIFE executed! \" + privateData);\n})();</code></pre>\n</div>"
    },
    {
      "id": "js-functions-sync",
      "section": "3. Functions",
      "title": "Synchronous Functions",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Functions</div>\n  <h2>3.2 Synchronous Functions</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Core Foundation</span>\n    <span class=\"gfg-time\">⏱️ 10 min read</span>\n  </div>\n\n  <h3>Understanding Synchronous Execution</h3>\n  <p>JavaScript is a single-threaded language, meaning it has exactly one Call Stack and one Memory Heap. By default, JavaScript executes code synchronously. Synchronous execution implies that the code is read and executed line-by-line, in the exact order it is written. The engine will not move on to the next line of code until the current line has completely finished executing.</p>\n\n  <p>In a synchronous environment, each task blocks the execution of the next task. This is straightforward and easy to reason about, but it introduces massive performance bottlenecks if a task takes too long.</p>\n\n  <h3>The Call Stack in Action</h3>\n  <p>The Call Stack is a data structure that records where in the program we are. If we step into a function, we push it onto the stack. If we return from a function, we pop it off the top of the stack. Let's look at an example:</p>\n  <pre><code>function first() {\n    console.log(\"Executing first\");\n    second();\n    console.log(\"Finished first\");\n}\n\nfunction second() {\n    console.log(\"Executing second\");\n}\n\nfirst();\n// Output Order:\n// Executing first\n// Executing second\n// Finished first</code></pre>\n  \n  <h3>The Problem with Blocking Code</h3>\n  <p>While synchronous code is predictable, it struggles with computationally heavy operations or tasks that involve waiting (like reading a large file or requesting data over a network). If you execute a heavy <code>while</code> loop synchronously on the main thread of a browser, the entire webpage will freeze. Users won't be able to click buttons, scroll, or type until the loop finishes.</p>\n  <pre><code>function blockThread() {\n    console.log(\"Starting a heavy task...\");\n    let start = Date.now();\n    // Simulate a 3-second block\n    while (Date.now() - start &lt; 3000) { }\n    console.log(\"Heavy task finished!\");\n}</code></pre>\n  <p>Because blocking the main thread destroys the user experience, JavaScript relies on asynchronous programming to handle long-running operations without stopping the entire program.</p>\n</div>"
    },
    {
      "id": "js-functions-async",
      "section": "3. Functions",
      "title": "Asynchronous Functions",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Functions</div>\n  <h2>3.3 Asynchronous Functions & The Event Loop</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Advanced</span>\n    <span class=\"gfg-time\">⏱️ 15 min read</span>\n  </div>\n\n  <h3>What is Asynchronous JavaScript?</h3>\n  <p>Asynchronous programming is a technique that enables your program to start a potentially long-running task and still be able to be responsive to other events while that task runs, rather than having to wait until that task has finished. Once that task has finished, your program is presented with the result.</p>\n  <p>Since JavaScript is strictly single-threaded, it achieves asynchronous behavior not by creating new threads, but by utilizing the browser's Web APIs (or Node.js C++ APIs), the Callback Queue, and the legendary Event Loop.</p>\n\n  <h3>How Asynchronous Code Works Under the Hood</h3>\n  <p>When you call an asynchronous function like <code>setTimeout</code> or <code>fetch</code>, the JavaScript engine hands the task over to the Web API environment and instantly moves on to execute the next line of synchronous code. It does not wait.</p>\n  <ol>\n    <li><strong>Web APIs:</strong> The browser handles the timer or the network request in the background.</li>\n    <li><strong>Callback Queue:</strong> Once the background task finishes, its associated callback function is pushed into the Callback Queue (also called the Task Queue).</li>\n    <li><strong>The Event Loop:</strong> The Event Loop constantly checks if the main Call Stack is empty. If the stack is completely empty, it takes the first callback from the Callback Queue and pushes it onto the Call Stack to be executed.</li>\n  </ol>\n\n  <h3>A Classic Example</h3>\n  <pre><code>console.log(\"1. Program Starts\");\n\nsetTimeout(() =&gt; {\n    console.log(\"3. Timeout Callback Executes\");\n}, 0);\n\nconsole.log(\"2. Program Ends\");\n\n// Output Order:\n// 1. Program Starts\n// 2. Program Ends\n// 3. Timeout Callback Executes</code></pre>\n  <p>Even though the timeout delay is 0 milliseconds, the callback is sent to the Web API and then the Callback Queue. It must wait for the synchronous code to completely finish before the Event Loop allows it onto the Call Stack.</p>\n</div>"
    },
    {
      "id": "js-functions-callbacks",
      "section": "3. Functions",
      "title": "Callbacks",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Functions</div>\n  <h2>3.4 Callbacks & Callback Hell</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Advanced</span>\n    <span class=\"gfg-time\">⏱️ 15 min read</span>\n  </div>\n\n  <h3>The Role of Callbacks</h3>\n  <p>A callback is simply a function passed as an argument to another function, intended to be executed after a specific event occurs or a task completes. Before Promises were introduced, callbacks were the primary way to manage asynchronous workflows in JavaScript.</p>\n\n  <h3>Synchronous vs Asynchronous Callbacks</h3>\n  <p>Callbacks aren't inherently asynchronous. For instance, the <code>map</code> and <code>filter</code> array methods use synchronous callbacks. The engine executes them immediately, pausing everything else.</p>\n  <pre><code>// Synchronous Callback\n[1, 2, 3].forEach(num =&gt; console.log(num));</code></pre>\n  <p>Asynchronous callbacks are used in environments like event listeners or file reading, where the callback is pushed to the Callback Queue and invoked later by the Event Loop.</p>\n\n  <h3>The Pyramid of Doom (Callback Hell)</h3>\n  <p>The major flaw of callbacks becomes apparent when you have multiple dependent asynchronous operations. If Operation B needs the result of Operation A, and Operation C needs the result of Operation B, you must nest the callbacks inside one another. This deep nesting creates a triangular shape in your code known as \"Callback Hell\".</p>\n  <pre><code>function fetchUserData(userId, callback) {\n    // Simulated DB Call\n    setTimeout(() =&gt; callback({ id: userId, name: \"Alice\" }), 1000);\n}\n\nfunction fetchUserPosts(user, callback) {\n    setTimeout(() =&gt; callback([\"Post 1\", \"Post 2\"]), 1000);\n}\n\n// Callback Hell Example\nfetchUserData(1, function(user) {\n    console.log(\"User fetched:\", user.name);\n    fetchUserPosts(user, function(posts) {\n        console.log(\"Posts fetched:\", posts.length);\n        // Imagine 5 more levels of nesting...\n    });\n});</code></pre>\n  <p>Callback hell leads to code that is notoriously difficult to read, maintain, and debug. Error handling is also fragmented, requiring separate <code>if (err)</code> checks at every single nested level. This architectural flaw is exactly why Promises were invented.</p>\n</div>"
    },
    {
      "id": "js-functions-promises",
      "section": "3. Functions",
      "title": "Promises",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Functions</div>\n  <h2>3.5 Promises</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Advanced</span>\n    <span class=\"gfg-time\">⏱️ 20 min read</span>\n  </div>\n\n  <h3>What is a Promise?</h3>\n  <p>Introduced in ES6 (2015), a Promise is an object representing the eventual completion or failure of an asynchronous operation. Instead of passing a callback function into an asynchronous routine, the routine returns a Promise object that you can attach handlers to. This fundamentally flattens the nested structure of callbacks.</p>\n\n  <h3>The Three States of a Promise</h3>\n  <ul>\n    <li><strong>Pending:</strong> The initial state. The operation has not completed yet.</li>\n    <li><strong>Fulfilled:</strong> The operation completed successfully, resulting in a value.</li>\n    <li><strong>Rejected:</strong> The operation failed, resulting in an error reason.</li>\n  </ul>\n  <p>Once a Promise is fulfilled or rejected, it is considered \"settled\" and its state cannot be changed.</p>\n\n  <h3>Creating and Consuming Promises</h3>\n  <p>You can create a Promise using the <code>new Promise()</code> constructor, which takes an executor function with <code>resolve</code> and <code>reject</code> arguments.</p>\n  <pre><code>const checkServer = new Promise((resolve, reject) =&gt; {\n    let isOnline = true;\n    setTimeout(() =&gt; {\n        if (isOnline) resolve(\"Server is online!\");\n        else reject(\"Server is down.\");\n    }, 1000);\n});\n\n// Consuming the Promise\ncheckServer\n    .then(message =&gt; console.log(\"Success:\", message))\n    .catch(error =&gt; console.error(\"Error:\", error))\n    .finally(() =&gt; console.log(\"Check complete.\"));</code></pre>\n\n  <h3>Chaining Promises</h3>\n  <p>Because the <code>.then()</code> method returns a brand new Promise, you can chain multiple asynchronous operations together sequentially. This flattens the dreaded \"Pyramid of Doom\" into a clean, vertical line of commands.</p>\n  <pre><code>fetchData()\n    .then(data =&gt; parseData(data))\n    .then(parsed =&gt; saveToDatabase(parsed))\n    .then(result =&gt; console.log(\"Saved successfully!\"))\n    .catch(error =&gt; console.error(\"Pipeline failed at some step:\", error));</code></pre>\n    <p>Notice how a single <code>.catch()</code> block at the end can handle errors originating from any of the chained promises, making error management vastly superior to callbacks.</p>\n</div>"
    },
    {
      "id": "js-functions-asyncawait",
      "section": "3. Functions",
      "title": "Async/Await",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Functions</div>\n  <h2>3.6 Async &amp; Await</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Modern JS</span>\n    <span class=\"gfg-time\">⏱️ 15 min read</span>\n  </div>\n\n  <h3>Syntactic Sugar for Promises</h3>\n  <p>Introduced in ES2017 (ES8), the <code>async</code> and <code>await</code> keywords provide a revolutionary way to write asynchronous code. They sit strictly on top of Promises, acting as \"syntactic sugar\". Their goal is to make asynchronous code read exactly like standard, synchronous code, completely eliminating the need for <code>.then()</code> and <code>.catch()</code> chains.</p>\n\n  <h3>The <code>async</code> Keyword</h3>\n  <p>Adding <code>async</code> before a function declaration explicitly ensures that the function will always return a Promise. Even if you just return a plain string, JavaScript automatically wraps it in a resolved Promise.</p>\n  <pre><code>async function getGreeting() {\n    return \"Hello World\"; // Equivalent to Promise.resolve(\"Hello World\")\n}\ngetGreeting().then(console.log);</code></pre>\n\n  <h3>The <code>await</code> Keyword</h3>\n  <p>The <code>await</code> keyword can only be used inside an <code>async</code> function. It literally pauses the execution of that specific function until the Promise it is waiting for settles. However, it does <strong>not</strong> block the main JavaScript thread; the rest of the application remains responsive.</p>\n  <pre><code>async function fetchAndLogUser() {\n    console.log(\"Request sent...\");\n    // Execution pauses here until the fetch resolves\n    const response = await fetch(\"https://jsonplaceholder.typicode.com/users/1\");\n    const user = await response.json();\n    console.log(\"User received:\", user.name);\n}</code></pre>\n\n  <h3>Error Handling</h3>\n  <p>One of the best features of Async/Await is that it allows you to use standard synchronous <code>try...catch</code> blocks for error handling. This unifies how you handle both synchronous and asynchronous errors in your codebase.</p>\n  <pre><code>async function safeFetch() {\n    try {\n        const response = await fetch(\"https://invalid-url.com\");\n        if (!response.ok) throw new Error(\"HTTP Status \" + response.status);\n        const data = await response.json();\n        return data;\n    } catch (error) {\n        console.error(\"Fetch failed gracefully:\", error.message);\n    }\n}</code></pre>\n</div>"
    },
    {
      "id": "js-functions-fetch",
      "section": "3. Functions",
      "title": "Fetch API",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Functions</div>\n  <h2>3.7 Fetch API & HTTP Methods</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Modern JS</span>\n    <span class=\"gfg-time\">⏱️ 15 min read</span>\n  </div>\n\n  <h3>Introduction to the Fetch API</h3>\n  <p>The Fetch API provides a JavaScript interface for accessing and manipulating parts of the protocol, such as requests and responses. It also provides a global <code>fetch()</code> method that provides an easy, logical way to fetch resources asynchronously across the network. Before the Fetch API, the standard way to do this was with the <code>XMLHttpRequest</code> (XHR) object, which had a clunky, callback-heavy API. The Fetch API uses Promises, which enables a simpler and cleaner API, avoiding callback hell and making it easy to remember.</p>\n\n  <h3>Making Basic GET Requests</h3>\n  <p>The most common use of <code>fetch()</code> is to retrieve data from a remote API. By default, <code>fetch()</code> makes a GET request. It takes one mandatory argument, the path to the resource you want to fetch, and returns a Promise that resolves to the Response to that request.</p>\n\n  <pre><code>fetch('https://jsonplaceholder.typicode.com/posts/1')\n    .then(response =&gt; {\n        // fetch() only rejects on network failure. \n        // We must manually check if the HTTP status is OK (200-299).\n        if (!response.ok) {\n            throw new Error(`HTTP error! status: ${response.status}`);\n        }\n        return response.json(); // Parses the JSON payload\n    })\n    .then(data =&gt; console.log('Data received:', data))\n    .catch(error =&gt; console.error('Fetch error:', error));</code></pre>\n\n  <h3>HTTP Methods Overview</h3>\n  <p>While GET requests are for retrieving data, modern web applications need to send data to the server, update existing data, and delete data. This is accomplished using different HTTP methods:</p>\n  <ul>\n    <li><strong>GET:</strong> Retrieve data from the server.</li>\n    <li><strong>POST:</strong> Send new data to the server (e.g., submitting a form, creating a new user).</li>\n    <li><strong>PUT / PATCH:</strong> Update existing data on the server.</li>\n    <li><strong>DELETE:</strong> Remove data from the server.</li>\n  </ul>\n\n  <h3>Sending Data with POST Requests</h3>\n  <p>To make a POST request, you need to pass an optional <code>init</code> object as the second argument to <code>fetch()</code>. This object allows you to control a number of different settings, such as the HTTP method, headers, and the body of the request.</p>\n\n  <pre><code>const newPost = {\n    title: 'Learning Fetch',\n    body: 'The Fetch API is modern and promise-based.',\n    userId: 1\n};\n\nfetch('https://jsonplaceholder.typicode.com/posts', {\n    method: 'POST', // Specify the HTTP method\n    headers: {\n        // Tell the server we are sending JSON data\n        'Content-Type': 'application/json',\n    },\n    // The body must be a string, so we serialize the JavaScript object\n    body: JSON.stringify(newPost)\n})\n.then(response =&gt; response.json())\n.then(data =&gt; console.log('Successfully created:', data))\n.catch(error =&gt; console.error('Error creating post:', error));</code></pre>\n\n  <h3>Handling Cross-Origin Requests (CORS)</h3>\n  <p>When you use <code>fetch()</code> to request data from a different domain than the one that served your webpage, you are making a Cross-Origin Resource Sharing (CORS) request. For security reasons, browsers restrict cross-origin HTTP requests initiated from scripts. The server must explicitly allow your domain by setting appropriate CORS headers (like <code>Access-Control-Allow-Origin</code>). If the server does not allow it, your <code>fetch()</code> call will fail with a CORS error, regardless of whether the URL is valid.</p>\n\n  <h3>Fetch with Async / Await</h3>\n  <p>Because <code>fetch()</code> returns a Promise, it is perfectly suited for the <code>async</code> / <code>await</code> syntax, which makes asynchronous code look highly readable and synchronous.</p>\n\n  <pre><code>async function getPostData() {\n    try {\n        const response = await fetch('https://jsonplaceholder.typicode.com/posts/1');\n        if (!response.ok) {\n            throw new Error(`HTTP error! Status: ${response.status}`);\n        }\n        const data = await response.json();\n        console.log(data);\n    } catch (error) {\n        console.error(\"Failed to fetch data:\", error);\n    }\n}</code></pre>\n</div>"
    },
    {
      "id": "js-functions-api-task",
      "section": "3. Functions",
      "title": "API Task",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Functions</div>\n  <h2>3.8 API Task (Fetching Products)</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Practical Task</span>\n    <span class=\"gfg-time\">⏱️ 20 min read</span>\n  </div>\n\n  <h3>Overview</h3>\n  <p>In this task, we bring together HTML, CSS, and JavaScript's Fetch API to create a dynamic product grid. We will fetch mock product data from an external API (<code>https://dummyjson.com/products</code>) and render it directly to the DOM.</p>\n\n  <h3>The HTML Structure</h3>\n  <p>The foundation is incredibly simple. We only need an empty container element with an ID that JavaScript can target.</p>\n  <pre><code>&lt;div id=\"layout\"&gt;&lt;/div&gt;</code></pre>\n\n  <h3>The CSS Layout (Fixing Alignment and Padding)</h3>\n  <p>To ensure our products look great across all devices, we use CSS Grid for the layout and Flexbox for the individual product cards. We've optimized the alignment and padding for a polished look.</p>\n  <pre><code>#layout {\n    display: grid;\n    /* Responsive columns that adjust automatically */\n    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));\n    gap: 24px;\n    padding: 24px;\n    max-width: 1200px;\n    margin: 0 auto; /* Center alignment */\n}\n\n.box {\n    display: flex;\n    flex-direction: column;\n    align-items: center;      /* Center items horizontally */\n    justify-content: space-between; /* Evenly space items */\n    background-color: #ffffff;\n    border-radius: 12px;\n    box-shadow: 0 4px 10px rgba(0,0,0,0.1);\n    padding: 24px;            /* Generous padding for breathing room */\n    text-align: center;\n    transition: transform 0.2s ease;\n}\n\n.box:hover {\n    transform: translateY(-5px); /* Subtle hover effect */\n}\n\n.box img {\n    max-width: 100%;\n    height: auto;\n    border-radius: 8px;\n    margin-bottom: 16px;\n}\n\n.box h1 {\n    font-size: 1.25rem;\n    color: #333;\n    margin-bottom: 8px;\n}\n\n.box h3 {\n    font-size: 1rem;\n    color: #666;\n    margin-bottom: 12px;\n    font-weight: 400;\n}\n\n.box .price {\n    font-size: 1.5rem;\n    font-weight: bold;\n    color: #10b981; /* Accent color for price */\n}</code></pre>\n\n  <h3>The JavaScript (Fetch &amp; Render)</h3>\n  <p>We use the <code>fetch()</code> method to grab the JSON data. Once the Promise resolves, we use the <code>.map()</code> array method to iterate over the products. For each product, we construct an HTML string using template literals and inject it into our layout container using <code>innerHTML +=</code>.</p>\n  \n  <div class=\"gfg-callout gfg-note\">\n    <h4>💡 Performance Note</h4>\n    <p>Using <code>element.innerHTML +=</code> inside a loop forces the browser to re-parse the entire HTML string on every iteration. For better performance in large apps, you should build the full HTML string first in a variable, and then assign it to <code>innerHTML</code> once at the end.</p>\n  </div>\n\n  <pre><code>let element = document.getElementById(\"layout\");\n\nfetch(\"https://dummyjson.com/products\")\n    .then((response) =&gt; {\n        // Step 1: Parse the response to JSON\n        return response.json();\n    })\n    .then((data) =&gt; {\n        // Step 2: Extract the products array and iterate\n        data.products.map((product) =&gt; {\n            // Step 3: Append constructed HTML into the DOM\n            element.innerHTML += `\n                &lt;div class=\"box\"&gt;\n                    &lt;img src=\"${product.thumbnail}\" alt=\"${product.title}\"&gt;\n                    &lt;h1&gt;${product.title}&lt;/h1&gt;\n                    &lt;h3&gt;${product.description}&lt;/h3&gt;\n                    &lt;span class=\"price\"&gt;$${product.price}&lt;/span&gt;\n                &lt;/div&gt;\n            `;\n        });\n    })\n    .catch((err) =&gt; {\n        console.error(\"Failed to fetch products:\", err);\n    });</code></pre>\n</div>"
    },
    {
      "id": "js-dom-manipulation",
      "section": "4. DOM & Events",
      "title": "DOM Manipulation",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; DOM &amp; Events</div>\n  <h2>DOM Manipulation</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Browser API</span>\n    <span class=\"gfg-time\">⏱️ 8 min read</span>\n    <span class=\"gfg-updated\">Updated: July 2026</span>\n  </div>\n\n  <p>The Document Object Model (DOM) is a programming interface for web documents. It represents the page so that programs can change the document structure, style, and content.</p>\n\n  <h3>Selecting Elements</h3>\n  <table class=\"gfg-table\">\n    <thead>\n      <tr>\n        <th>Method</th>\n        <th>Description</th>\n      </tr>\n    </thead>\n    <tbody>\n      <tr>\n        <td><code>getElementById(\"id\")</code></td>\n        <td>Returns a single element with the matching ID.</td>\n      </tr>\n      <tr>\n        <td><code>querySelector(\".class\")</code></td>\n        <td>Returns the <em>first</em> element matching the CSS selector.</td>\n      </tr>\n      <tr>\n        <td><code>querySelectorAll(\"div\")</code></td>\n        <td>Returns a NodeList of <em>all</em> elements matching the selector.</td>\n      </tr>\n    </tbody>\n  </table>\n\n  <h3>Modifying Content & Styles</h3>\n  <pre><code>// 1. Select the element\nconst heading = document.querySelector(\"#main-title\");\n\n// 2. Change Text\nheading.textContent = \"New Title Updated via JS!\";\n\n// 3. Change HTML\nconst box = document.querySelector(\".box\");\nbox.innerHTML = \"&lt;strong&gt;Bold Text!&lt;/strong&gt;\";\n\n// 4. Change Styles\nheading.style.color = \"#10b981\";\nheading.style.fontSize = \"3rem\";\nheading.style.display = \"none\"; // Hide element</code></pre>\n\n  <h3>Creating and Appending Elements</h3>\n  <pre><code>// Create a new paragraph\nconst newPara = document.createElement(\"p\");\nnewPara.textContent = \"I was created dynamically.\";\nnewPara.classList.add(\"text-muted\");\n\n// Append it to a container\nconst container = document.getElementById(\"content-div\");\ncontainer.appendChild(newPara);</code></pre>\n</div>"
    },
    {
      "id": "js-events",
      "section": "4. DOM & Events",
      "title": "Event Listeners",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; DOM &amp; Events</div>\n  <h2>Event Listeners</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Interactive</span>\n    <span class=\"gfg-time\">⏱️ 7 min read</span>\n    <span class=\"gfg-updated\">Updated: July 2026</span>\n  </div>\n\n  <p>JavaScript can execute code when events occur (like clicking a button, typing in a field, or loading a page). We use the <code>addEventListener</code> method to attach event handlers to elements.</p>\n\n  <div class=\"gfg-syntax-box\">\n    <div class=\"syntax-title\">addEventListener Syntax</div>\n    <pre><code>element.addEventListener(event, function, useCapture);</code></pre>\n  </div>\n\n  <h3>Common Events</h3>\n  <ul>\n    <li><strong>Mouse:</strong> <code>click</code>, <code>dblclick</code>, <code>mouseenter</code>, <code>mouseleave</code></li>\n    <li><strong>Keyboard:</strong> <code>keydown</code>, <code>keyup</code>, <code>keypress</code></li>\n    <li><strong>Form:</strong> <code>submit</code>, <code>change</code>, <code>input</code>, <code>focus</code></li>\n    <li><strong>Window:</strong> <code>load</code>, <code>resize</code>, <code>scroll</code></li>\n  </ul>\n\n  <h3>Event Listener Example</h3>\n  <pre><code>const btn = document.querySelector(\"#submit-btn\");\n\nbtn.addEventListener(\"click\", function(event) {\n  // Prevent default form submission behavior if needed\n  event.preventDefault();\n  \n  // 'event.target' refers to the element that triggered the event\n  event.target.style.backgroundColor = \"green\";\n  event.target.textContent = \"Clicked!\";\n  \n  console.log(\"Button was clicked at coordinates:\", event.clientX, event.clientY);\n});</code></pre>\n\n  <div class=\"gfg-callout gfg-tip\">\n    <h4>💡 Event Delegation</h4>\n    <p>Instead of adding listeners to multiple child elements (like list items), add a single listener to their parent element and check <code>event.target</code>. This is more efficient and handles dynamically added children.</p>\n  </div>\n</div>"
    },
    {
      "id": "js-async",
      "section": "5. Advanced Concepts",
      "title": "Asynchronous JavaScript",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Advanced</div>\n  <h2>Asynchronous JavaScript: Promises & Async/Await</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Advanced</span>\n    <span class=\"gfg-time\">⏱️ 12 min read</span>\n    <span class=\"gfg-updated\">Updated: July 2026</span>\n  </div>\n\n  <p>JavaScript is single-threaded. To prevent blocking the main thread during long operations (like fetching data from an API), JavaScript uses asynchronous patterns.</p>\n\n  <h3>1. Promises</h3>\n  <p>A Promise is an object representing the eventual completion or failure of an asynchronous operation.</p>\n  <pre><code>const myPromise = new Promise((resolve, reject) => {\n  let success = true;\n  \n  setTimeout(() => {\n    if(success) {\n      resolve(\"Data fetched successfully!\");\n    } else {\n      reject(\"Error fetching data.\");\n    }\n  }, 2000);\n});\n\nmyPromise\n  .then(result => console.log(result))\n  .catch(error => console.error(error))\n  .finally(() => console.log(\"Operation complete.\"));</code></pre>\n\n  <h3>2. Fetch API</h3>\n  <p>The <code>fetch()</code> method is a modern way to make network requests. It returns a Promise.</p>\n  <pre><code>fetch('https://jsonplaceholder.typicode.com/users/1')\n  .then(response => {\n    if (!response.ok) throw new Error(\"Network error\");\n    return response.json(); // Parses JSON and returns another promise\n  })\n  .then(data => console.log(\"User Name:\", data.name))\n  .catch(error => console.error(\"Fetch Error:\", error));</code></pre>\n\n  <h3>3. Async / Await (ES8)</h3>\n  <p><code>async/await</code> is syntactic sugar on top of Promises, making asynchronous code look and behave a little more like synchronous code.</p>\n  <div class=\"gfg-syntax-box\">\n    <div class=\"syntax-title\">Async/Await Example</div>\n    <pre><code>async function getUserData() {\n  try {\n    const response = await fetch('https://jsonplaceholder.typicode.com/users/1');\n    if (!response.ok) {\n      throw new Error(`HTTP error! status: ${response.status}`);\n    }\n    const data = await response.json();\n    console.log(\"User:\", data);\n  } catch (error) {\n    console.error(\"Error occurred:\", error);\n  }\n}\n\ngetUserData();</code></pre>\n  </div>\n</div>"
    },
    {
      "id": "js-es6",
      "section": "5. Advanced Concepts",
      "title": "ES6+ Features",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Advanced</div>\n  <h2>Modern JavaScript (ES6+ Features)</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\" style=\"background: #eab308; color: #422006;\">Modern JS</span>\n    <span class=\"gfg-time\">⏱️ 10 min read</span>\n    <span class=\"gfg-updated\">Updated: July 2026</span>\n  </div>\n\n  <p>ECMAScript 2015 (ES6) and subsequent versions introduced powerful new syntax and features that make writing JavaScript easier and cleaner.</p>\n\n  <h3>1. Destructuring Assignment</h3>\n  <p>Unpack values from arrays or properties from objects into distinct variables.</p>\n  <pre><code>// Object Destructuring\nconst user = { name: \"Alice\", age: 28, city: \"Paris\" };\nconst { name, city } = user;\nconsole.log(name, city); // \"Alice Paris\"\n\n// Array Destructuring\nconst colors = [\"Red\", \"Green\", \"Blue\"];\nconst [primary, secondary] = colors;\nconsole.log(primary); // \"Red\"</code></pre>\n\n  <h3>2. Spread & Rest Operators (...)</h3>\n  <pre><code>// Spread (Expand arrays/objects)\nconst arr1 = [1, 2];\nconst arr2 = [...arr1, 3, 4]; // [1, 2, 3, 4]\n\nconst obj1 = { a: 1, b: 2 };\nconst obj2 = { ...obj1, c: 3 }; // { a: 1, b: 2, c: 3 }\n\n// Rest (Gather remaining arguments into an array)\nfunction sum(...numbers) {\n  return numbers.reduce((acc, val) => acc + val, 0);\n}\nconsole.log(sum(1, 2, 3, 4)); // 10</code></pre>\n\n  <h3>3. Template Literals</h3>\n  <p>Use backticks (`) to allow embedded expressions and multi-line strings.</p>\n  <pre><code>const product = \"Laptop\";\nconst price = 999;\n\n// ES5 way\nconst msg1 = \"The \" + product + \" costs $\" + price + \".\";\n\n// ES6 Template Literal\nconst msg2 = `The ${product} costs $${price}.`;\n\n// Multi-line\nconst html = `\n  &lt;div&gt;\n    &lt;h1&gt;${product}&lt;/h1&gt;\n  &lt;/div&gt;\n`;</code></pre>\n</div>"
    },
    {
      "id": "js-quiz",
      "section": "6. Test",
      "title": "Quiz",
      "quiz": [
        {
          "id": 1,
          "question": "What will 'typeof NaN' output in JavaScript?",
          "code": null,
          "options": [
            "'undefined'",
            "'nan'",
            "'number'",
            "'object'"
          ],
          "correct": 2,
          "explanation": "In JavaScript, NaN ('Not-a-Number') is a numeric data value representing an unrepresentable or undefined arithmetic outcome, so its type is 'number'."
        },
        {
          "id": 2,
          "question": "What is the primary difference between 'let' and 'var'?",
          "code": null,
          "options": [
            "'var' is block-scoped while 'let' is function-scoped",
            "'let' is block-scoped and not accessible before declaration (TDZ), whereas 'var' is function-scoped and hoisted with undefined",
            "'let' cannot be reassigned",
            "There is no difference"
          ],
          "correct": 1,
          "explanation": "'let' has block scope and resides in the Temporal Dead Zone (TDZ) before declaration, preventing bugs from accessing uninitialized variables."
        },
        {
          "id": 3,
          "question": "Which method creates a new array with the results of calling a provided function on every element?",
          "code": null,
          "options": [
            "forEach()",
            "filter()",
            "map()",
            "reduce()"
          ],
          "correct": 2,
          "explanation": "Array.prototype.map() transforms each element of the input array and returns a new array without mutating the original array."
        },
        {
          "id": 4,
          "question": "What does the '===' strict equality operator check that '==' does not?",
          "code": null,
          "options": [
            "Memory reference only",
            "Value equality with implicit type coercion",
            "Both value and data type without type coercion",
            "Object prototypes only"
          ],
          "correct": 2,
          "explanation": "Strict equality (===) requires both operands to possess identical types and values, eliminating unpredictable type coercion quirks of loose equality (==)."
        },
        {
          "id": 5,
          "question": "In an arrow function, how is the 'this' value determined?",
          "code": null,
          "options": [
            "Bound to the object invoking the function",
            "Always points to global window/globalThis",
            "Lexically inherited from the enclosing lexical execution context",
            "Bound via call/apply/bind only"
          ],
          "correct": 2,
          "explanation": "Arrow functions do not have their own 'this' binding. They lexically resolve 'this' from the outer surrounding scope in which they were created."
        }
      ],
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Test</div>\n  <h2>JavaScript Assessment: Practice Quiz</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\">Knowledge Assessment</span>\n    <span class=\"gfg-time\">⏱️ 15 min quiz</span>\n  </div>\n\n  <p>Evaluate your mastery of core JavaScript concepts with the assessment questions below. Review the options, test your understanding, and expand the explanation panels to verify your answers.</p>\n\n  <div class=\"test-section-wrapper\">\n      <!-- Question 1 -->\n      <div class=\"test-card\">\n        <div class=\"test-card-header\">\n          <h3 class=\"test-card-title\">Question 1: What will 'typeof NaN' output in JavaScript?</h3>\n          <span class=\"test-badge test-badge-medium\">5 Points</span>\n        </div>\n        <div class=\"test-options-list\">\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">A)</span>\n            <span>'undefined'</span>\n            \n          </div>\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">B)</span>\n            <span>'nan'</span>\n            \n          </div>\n          <div class=\"test-option-row correct\">\n            <span style=\"font-weight: 700; color: #34d399;\">C)</span>\n            <span>'number'</span>\n            <span style=\"margin-left: auto; font-size: 0.8rem; font-weight: 700; color: #34d399;\">✓ Correct</span>\n          </div>\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">D)</span>\n            <span>'object'</span>\n            \n          </div>\n        </div>\n        <details class=\"test-details\">\n          <summary>💡 View Answer &amp; Detailed Explanation</summary>\n          <div class=\"test-details-content\">\n            <p><strong>Correct Answer: Option C</strong></p>\n            <p>In JavaScript, NaN ('Not-a-Number') is a numeric data value representing an unrepresentable or undefined arithmetic outcome, so its type is 'number'.</p>\n          </div>\n        </details>\n      </div>\n\n      <!-- Question 2 -->\n      <div class=\"test-card\">\n        <div class=\"test-card-header\">\n          <h3 class=\"test-card-title\">Question 2: What is the primary difference between 'let' and 'var'?</h3>\n          <span class=\"test-badge test-badge-medium\">5 Points</span>\n        </div>\n        <div class=\"test-options-list\">\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">A)</span>\n            <span>'var' is block-scoped while 'let' is function-scoped</span>\n            \n          </div>\n          <div class=\"test-option-row correct\">\n            <span style=\"font-weight: 700; color: #34d399;\">B)</span>\n            <span>'let' is block-scoped and not accessible before declaration (TDZ), whereas 'var' is function-scoped and hoisted with undefined</span>\n            <span style=\"margin-left: auto; font-size: 0.8rem; font-weight: 700; color: #34d399;\">✓ Correct</span>\n          </div>\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">C)</span>\n            <span>'let' cannot be reassigned</span>\n            \n          </div>\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">D)</span>\n            <span>There is no difference</span>\n            \n          </div>\n        </div>\n        <details class=\"test-details\">\n          <summary>💡 View Answer &amp; Detailed Explanation</summary>\n          <div class=\"test-details-content\">\n            <p><strong>Correct Answer: Option B</strong></p>\n            <p>'let' has block scope and resides in the Temporal Dead Zone (TDZ) before declaration, preventing bugs from accessing uninitialized variables.</p>\n          </div>\n        </details>\n      </div>\n\n      <!-- Question 3 -->\n      <div class=\"test-card\">\n        <div class=\"test-card-header\">\n          <h3 class=\"test-card-title\">Question 3: Which method creates a new array with the results of calling a provided function on every element?</h3>\n          <span class=\"test-badge test-badge-medium\">5 Points</span>\n        </div>\n        <div class=\"test-options-list\">\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">A)</span>\n            <span>forEach()</span>\n            \n          </div>\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">B)</span>\n            <span>filter()</span>\n            \n          </div>\n          <div class=\"test-option-row correct\">\n            <span style=\"font-weight: 700; color: #34d399;\">C)</span>\n            <span>map()</span>\n            <span style=\"margin-left: auto; font-size: 0.8rem; font-weight: 700; color: #34d399;\">✓ Correct</span>\n          </div>\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">D)</span>\n            <span>reduce()</span>\n            \n          </div>\n        </div>\n        <details class=\"test-details\">\n          <summary>💡 View Answer &amp; Detailed Explanation</summary>\n          <div class=\"test-details-content\">\n            <p><strong>Correct Answer: Option C</strong></p>\n            <p>Array.prototype.map() transforms each element of the input array and returns a new array without mutating the original array.</p>\n          </div>\n        </details>\n      </div>\n\n      <!-- Question 4 -->\n      <div class=\"test-card\">\n        <div class=\"test-card-header\">\n          <h3 class=\"test-card-title\">Question 4: What does the '===' strict equality operator check that '==' does not?</h3>\n          <span class=\"test-badge test-badge-medium\">5 Points</span>\n        </div>\n        <div class=\"test-options-list\">\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">A)</span>\n            <span>Memory reference only</span>\n            \n          </div>\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">B)</span>\n            <span>Value equality with implicit type coercion</span>\n            \n          </div>\n          <div class=\"test-option-row correct\">\n            <span style=\"font-weight: 700; color: #34d399;\">C)</span>\n            <span>Both value and data type without type coercion</span>\n            <span style=\"margin-left: auto; font-size: 0.8rem; font-weight: 700; color: #34d399;\">✓ Correct</span>\n          </div>\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">D)</span>\n            <span>Object prototypes only</span>\n            \n          </div>\n        </div>\n        <details class=\"test-details\">\n          <summary>💡 View Answer &amp; Detailed Explanation</summary>\n          <div class=\"test-details-content\">\n            <p><strong>Correct Answer: Option C</strong></p>\n            <p>Strict equality (===) requires both operands to possess identical types and values, eliminating unpredictable type coercion quirks of loose equality (==).</p>\n          </div>\n        </details>\n      </div>\n\n      <!-- Question 5 -->\n      <div class=\"test-card\">\n        <div class=\"test-card-header\">\n          <h3 class=\"test-card-title\">Question 5: In an arrow function, how is the 'this' value determined?</h3>\n          <span class=\"test-badge test-badge-medium\">5 Points</span>\n        </div>\n        <div class=\"test-options-list\">\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">A)</span>\n            <span>Bound to the object invoking the function</span>\n            \n          </div>\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">B)</span>\n            <span>Always points to global window/globalThis</span>\n            \n          </div>\n          <div class=\"test-option-row correct\">\n            <span style=\"font-weight: 700; color: #34d399;\">C)</span>\n            <span>Lexically inherited from the enclosing lexical execution context</span>\n            <span style=\"margin-left: auto; font-size: 0.8rem; font-weight: 700; color: #34d399;\">✓ Correct</span>\n          </div>\n          <div class=\"test-option-row\">\n            <span style=\"font-weight: 700; color: var(--text-dim);\">D)</span>\n            <span>Bound via call/apply/bind only</span>\n            \n          </div>\n        </div>\n        <details class=\"test-details\">\n          <summary>💡 View Answer &amp; Detailed Explanation</summary>\n          <div class=\"test-details-content\">\n            <p><strong>Correct Answer: Option C</strong></p>\n            <p>Arrow functions do not have their own 'this' binding. They lexically resolve 'this' from the outer surrounding scope in which they were created.</p>\n          </div>\n        </details>\n      </div>\n  </div>\n</div>"
    },
    {
      "id": "js-interview-questions",
      "section": "6. Test",
      "title": "Interview Questions",
      "content": "<div class=\"course-topic\">\n  <div class=\"gfg-breadcrumb\">Tutorials &gt; Web Development &gt; JavaScript &gt; Test</div>\n  <h2>JavaScript Interview Questions &amp; Answers</h2>\n  <div class=\"gfg-meta\">\n    <span class=\"gfg-tag\">Interview Preparation</span>\n    <span class=\"gfg-time\">⏱️ 20 min read</span>\n  </div>\n\n  <p>Curated technical interview questions covering foundational principles, edge cases, and high-level architectural patterns frequently encountered in JavaScript developer interviews.</p>\n\n  <div class=\"test-section-wrapper\">\n      <!-- Question 1 -->\n      <div class=\"test-card\">\n        <div class=\"test-card-header\">\n          <h3 class=\"test-card-title\">Q1: What is a Closure in JavaScript and give an example?</h3>\n          <span class=\"test-badge test-badge-easy\">Easy</span>\n        </div>\n        <details class=\"test-details\" open>\n          <summary>📖 View Technical Answer &amp; Deep-Dive</summary>\n          <div class=\"test-details-content\">\n            A closure is the combination of a function bundled together with references to its surrounding state (the lexical environment). In JavaScript, closures allow an inner function to access variables from its outer function's scope even after the outer function has finished executing.<br><br><pre><code>function createCounter() {\n  let count = 0;\n  return {\n    increment: () => ++count,\n    getCount: () => count\n  };\n}\nconst counter = createCounter();\nconsole.log(counter.increment()); // 1\nconsole.log(counter.getCount());  // 1</code></pre>Closures are used for data privacy/encapsulation, memoization, and currying.\n          </div>\n        </details>\n      </div>\n\n      <!-- Question 2 -->\n      <div class=\"test-card\">\n        <div class=\"test-card-header\">\n          <h3 class=\"test-card-title\">Q2: Explain the JavaScript Event Loop (Call Stack, Microtasks, Macrotasks).</h3>\n          <span class=\"test-badge test-badge-medium\">Medium</span>\n        </div>\n        <details class=\"test-details\" open>\n          <summary>📖 View Technical Answer &amp; Deep-Dive</summary>\n          <div class=\"test-details-content\">\n            JavaScript is single-threaded, executing one operation at a time on its Call Stack.<br><br><strong>Execution Order:</strong><ol><li><strong>Call Stack:</strong> Synchronous code executes immediately to completion.</li><li><strong>Microtask Queue:</strong> High-priority asynchronous callbacks (e.g. <code>Promise.then</code>, <code>catch</code>, <code>finally</code>, <code>queueMicrotask</code>, <code>MutationObserver</code>). The event loop empties the entire microtask queue before moving forward.</li><li><strong>Macrotask Queue (Task Queue):</strong> Standard asynchronous tasks (e.g. <code>setTimeout</code>, <code>setInterval</code>, DOM events). The event loop processes one macrotask per tick, then checks microtasks again.</li></ol>\n          </div>\n        </details>\n      </div>\n\n      <!-- Question 3 -->\n      <div class=\"test-card\">\n        <div class=\"test-card-header\">\n          <h3 class=\"test-card-title\">Q3: What is the difference between Promise.all, Promise.allSettled, and Promise.race?</h3>\n          <span class=\"test-badge test-badge-medium\">Medium</span>\n        </div>\n        <details class=\"test-details\" open>\n          <summary>📖 View Technical Answer &amp; Deep-Dive</summary>\n          <div class=\"test-details-content\">\n            <ul><li><strong>Promise.all([p1, p2]):</strong> Resolves when ALL promises resolve; rejects immediately if ANY single promise rejects ('fail-fast').</li><li><strong>Promise.allSettled([p1, p2]):</strong> Waits for ALL promises to complete, regardless of resolution or rejection, returning an array of outcome objects <code>{status, value | reason}</code>.</li><li><strong>Promise.race([p1, p2]):</strong> Settles (resolves or rejects) as soon as the FIRST promise settles.</li><li><strong>Promise.any([p1, p2]):</strong> Resolves as soon as the FIRST promise succeeds (ignores errors unless ALL reject).</li></ul>\n          </div>\n        </details>\n      </div>\n\n      <!-- Question 4 -->\n      <div class=\"test-card\">\n        <div class=\"test-card-header\">\n          <h3 class=\"test-card-title\">Q4: Explain Prototypal Inheritance and the Prototype Chain.</h3>\n          <span class=\"test-badge test-badge-hard\">Hard</span>\n        </div>\n        <details class=\"test-details\" open>\n          <summary>📖 View Technical Answer &amp; Deep-Dive</summary>\n          <div class=\"test-details-content\">\n            In JavaScript, objects have an internal link to another object called their prototype (accessed via <code>Object.getPrototypeOf(obj)</code> or <code>__proto__</code>).<br><br>When trying to access a property or method on an object:<ul><li>If the object has the property, it is returned.</li><li>If not, JavaScript traverses up the prototype chain to check the object's prototype.</li><li>This search continues upward until either the property is found or the chain ends at <code>Object.prototype.__proto__</code>, which is <code>null</code>.</li></ul>ES6 classes are syntactic sugar built directly over this prototypal inheritance model.\n          </div>\n        </details>\n      </div>\n  </div>\n</div>"
    }
  ]
}